# Requirements

Optional host dependencies for skills in this repo. Install from the **ai-tooling** root (or from each bundle’s folder).

| Bundle | Skill | Install |
|--------|-------|---------|
| `pdf-summarizer/` | `pdf-summarizer` | [`pdf-summarizer/README.md`](pdf-summarizer/README.md) |

After installing a bundle, sync skills with [`../ai-assistant-kit/scripts/install-to-home.sh`](../ai-assistant-kit/scripts/install-to-home.sh) if you have not already.
